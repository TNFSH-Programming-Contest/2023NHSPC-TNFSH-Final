{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build LaTeX - XeLaTeX",
            "type": "shell",
            "command": "xelatex",
            "args": ["--shell-escape", "${file}"],
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "options": {
                "cwd": "${fileDirname}"
            }
        }
    ]
}